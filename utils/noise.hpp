#ifndef UTILS_NOISE_HPP
#define UTILS_NOISE_HPP

int randomGradient(int, int);
float perlin(float, float);
int dotGridGradient(int, int, float, float);
float interpolate(float, float, float);

#endif // UTILS_NOISE_HPP
